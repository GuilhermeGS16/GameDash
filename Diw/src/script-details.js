
// Criar método fetchGame(gameId) que recebe o id do jogo e retorna os detalhes

// Criar método de render para as informações

window.onload = function () {
/*
    Buscar o search da url (window.location.search)
    Criar uma instancia de UrlSearchParams passando o window.location.search e armazenar na const params (const params = new URLSearchParams(window.location.search))
    Pegar o parametro id utilizando params.get(id) e armazenar em uma variável
    fetchGame(id).then(renderGame)
*/

}